FROM python:3.12-slim

# Z harness runtime
WORKDIR /app
COPY harness.py pyproject.toml requirements.txt /app/
COPY src/ /app/src/
RUN pip install --no-cache-dir -r requirements.txt && pip install --no-cache-dir -e /app/

# Config mounted at runtime, workspace at /workspace
ENTRYPOINT ["z"]
